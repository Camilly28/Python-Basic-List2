
# circunferencia, diametro e circulo
n=float(input())

template = '{:.1f}'
saida = template.format(n)
print(saida)

template = '{:.2f}'
saida = template.format(n)
print(saida)

template = '{:.3f}'
saida = template.format(n)
print(saida)

